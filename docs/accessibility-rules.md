# Accessibility Rules

All controls require:

- label
- aria-label when needed

Buttons must have text.

Forms must be keyboard accessible.

Use:

button

instead of clickable divs.

Maintain contrast compliance.

Inputs must display validation messages.